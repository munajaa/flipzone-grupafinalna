
[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

# 1. DOPUSTI PRISTUP AKO KORISNIK IMA PAID ROLU
[[redirects]]
  from = "/app/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["paid"]}

# 2. SPA FALLBACK (Sve rute šalji na index.html)
# Ovo osigurava da React obradi rute i da se ne gubi #invite_token
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
