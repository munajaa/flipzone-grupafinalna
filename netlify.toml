
[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

# Jednostavan SPA fallback
# Netlify Identity će sam obraditi tokene na / ruti
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
